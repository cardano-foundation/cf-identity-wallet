version: "3.9"
services:
  webrtc-relay:
    container_name: webrtc-relay
    build:
      context: ./webrtc-relay
      dockerfile: Dockerfile
    container_name: webrtc-relay
    tty: true
    ports:
      - 51986:51986
    restart: always
